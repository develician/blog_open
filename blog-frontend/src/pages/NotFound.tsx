import * as React from 'react';
import NotFoundContainer from 'containers/common/NotFoundContainer';

// interface NotFoundProps {}

const NotFound: React.SFC<{}> = () => {
  return <NotFoundContainer />;
};

export default NotFound;
